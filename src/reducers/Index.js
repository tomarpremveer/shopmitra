import { SIGN_IN, SIGN_OUT } from "../types";

export const REDUCERS = (state = {}, action) => {
  switch (action.type) {
    case SIGN_IN:
      break;
    case SIGN_OUT:
      break;
    default:
      break;
  }
};
